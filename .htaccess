<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all requests to the Laravel public folder (front controller lives there).
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L,NC]
</IfModule>
